#include "modes.h"
/*-----------------------------------
 *-----WELCOME TO HOOKAH 0S 1.0!-----
 *-----------------------------------
 *-------------developed by XeWe-----
 *-----------------------------------
 *
 *C-circular pattern 
 *L-linear pattern 
 *I - inside 
 *O - out
 *T - thin
 *F - fat
 *
 *(recommended parameters in brackets)
 *
 * fill_mode(color, delay (4000), 'C/L', 'I/O');
 * 
 * fade_mode(color, delay (10000));
 * 
 * mix_mode(delay (120000), multiplier (10), green reduce (6), 'R/G/B', 'R/G/B');
 * 
 * random_mode(color, delay (4000));
 * 
 * gazer_mode(color, delay (7000));
 * 
 * scan_mode(color, delay (3000), 'T/F');
 * 
 * radar_mode(circle color, background color, delay (2500));
 * 
 * snow_mode(flakes color, background color, delay (400));
 * 
 * rainbow_mode(delay (10000), green reduce (4)) 
 *
 *demo_mode();
 */
 
void loop() 
{
  demo_mode();
}
